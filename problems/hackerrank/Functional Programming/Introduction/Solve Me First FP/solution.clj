(defn solve-me-first [x y]
    (+ x y))

(let [
    a (Integer/parseInt (read-line))
    b (Integer/parseInt (read-line))
    ]
    (println (solve-me-first a b)))



(comment This is HackerRank's solution

    (defn solveMeFirst [x y]
        (+ x y))


    (def a (read-line))
    (def b (read-line))

    (println (solveMeFirst (Integer/parseInt a) (Integer/parseInt b)))

)
